<header>

  <style>
   .logoBanner {
    /* background-color: white; */
    height: 125px;
    width: 35%;
    max-width: 250px;
    padding: 10px; 
    margin: 0 0 0 3%;
    position: fixed;
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 2;
  }

  .logo {
    background-image: url(/images/imagery/Logo.png);
    background-size: contain;
    height: 100%;
    width: 100%;
    background-repeat: no-repeat;
    background-position: center;
    filter: invert(1);
  }

  /* Add a pseudo-element to the top */
  /* .logoBanner::before {
    content: "";
    position: absolute;
    top: -40px;
    left: 0px;
    height: 40px;
    width: 40px;
    background: transparent;
    border-bottom-left-radius: 50%; 
    box-shadow: -20px 20px 0 0 var(--white); 
  } */

  /* Keep the original pseudo-element */
  /* .logoBanner::after {
    content: "";
    position: absolute;
    bottom: -40px;
    left: 0;
    height: 40px;
    width: 40px;
    background: transparent;
    border-top-left-radius: 50%;
    box-shadow: -20px -20px 0 0 var(--white);
  } */
  @media (max-width: 767px) {
    .logoBanner{
      width: 20%;
      height: 10%;
    }   
    /* .radialMenu {
      right: 50px !important;
    } */
     .menuOption{
      height: 9vh !important;
     }
    .menuOption a {
      font-size: 0.75rem !important;
    }
    /* .menuOption {
      width: 50vw !important;  
      height: 8vh !important;  
    } */
  }
  .radialMenu {
  position: fixed;
  right: 0;
  top: -30px;
  transition: transform 0.75s ease-out;
  border-radius: 0 0 10px 10px;
  display: flex; 
  z-index: 100;
}

.menuOption::after {
  content: "";
  position: absolute;
  top: 0;
  left: 0; /* Align to the top-left of each menuOption */
  width: 100%;  /* Ensure it matches the width of the individual option */
  height: 100%; /* Ensure it matches the height of the individual option */
  background-image: url(/images/imagery/LightWoodTexture.jpg); 
  opacity: 0.5;
  border-radius: 0 0 10px 10px; /* Apply rounded corners to the bottom */
}

.menuOption {
  position: relative;  /* Set position relative to its own container */
  height: 13vh;
  width: auto;  /* Ensure each option width fits content */
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 10px;
  border-radius: 0;
  transition: transform 0.3s ease-out;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
}

.option1 {
  border-radius:  0 0 10px 10px;   
  background: repeating-linear-gradient(
    to bottom,  
    #F0C7A3,
#F0C7A3 20%,
#E5A98B 20%,
#E5A98B 40%,
#D1A67C 40%,
#D1A67C 60%,
#B8775A 60%,
#B8775A 80%,
#9D4F39 80%,
#9D4F39 100%
  );
  transform-origin: left center;
  transition: transform 0.3s ease-out;
}

.option2 {
  border-radius: 0 0 10px 10px;  
  background: repeating-linear-gradient(
    to bottom,  
    #F0C7A3,
#F0C7A3 20%,
#E5A98B 20%,
#E5A98B 40%,
#D1A65A 40%,
#D1A65A 60%,
#B88A3A 60%,
#B88A3A 80%,
#9D7419 80%,
#9D7419 100%
  );
  transform-origin: left center;
  transition: transform 0.3s ease-out;
}

.option3 {
  border-radius: 0 0 10px 10px;   
  background: repeating-linear-gradient(
    to bottom,  
    #6E4B3A,
#6E4B3A 20%,
#5E3B2A 20%,
#5E3B2A 40%,
#4F2E1B 40%,
#4F2E1B 60%,
#3F1E0F 60%,
#3F1E0F 80%,
#2F1100 80%,
#2F1100 100%
  );
  transform-origin: left center;
  transition: transform 0.3s ease-out;
}

.option4 {
  border-radius:  0 0 10px 10px;    
  background: repeating-linear-gradient(
    to bottom,  
    #8C5A3B,
#8C5A3B 20%,
#7A4A2C 20%,
#7A4A2C 40%,
#6A3A1E 40%,
#6A3A1E 60%,
#5A2A0F 60%,
#5A2A0F 80%,
#4A1A00 80%,
#4A1A00 100%
  );
  transform-origin: left center;
  transition: transform 0.3s ease-out;
}

.option1:hover {
  transform-origin: left center;
  transform:translateY(3vh);
}

.option2:hover {
  transform-origin: left center;
  transform: translateY(3vh);
}

.option3:hover {
  transform-origin: left center;
  transform: translateY(3vh);
}

.option4:hover {
  transform-origin: left center;
  transform: translateY(3vh);
}

.menuOption a {
  color: rgba(125, 102, 72, 0.7);
  text-decoration: none;
  display: block;
  white-space: nowrap;
  padding: 5px 2vw;
  font-size: 1.5rem;
  font-weight: 800;
  text-shadow: 0 0 1px rgba(55, 22, 6, 0.75);
  font-family: Futura Medium BT, sans-serif;
  mix-blend-mode: multiply;
  z-index: 1;
}

  </style>
<!-- Radial Menu -->
<div class="radialMenu" id="radialMenu">
  <!-- <div class="menuOption option1"><a href="/">Home</a></div> -->
  <div class="menuOption option2"><a href="/about">About</a></div>
  <div class="menuOption option3"><a href="/gallery">Gallery</a></div>
  <div class="menuOption option4"><a href="/contact">Contact Us</a></div>
</div>

<a href="/">
<div class="logoBanner">
    <div class="logo" id="logo"></div>
</div>
</a>
    <script>
      // Get the current page path (e.g., "/gallery", "/home", etc.)
      const pagePath = window.location.pathname;

      // Save the scroll position when the user scrolls
      window.addEventListener('scroll', () => {
          sessionStorage.setItem(`scrollPosition_${pagePath}`, window.scrollY);
      });

      // Retrieve and apply the scroll position on page load
      window.addEventListener('load', () => {
          const savedScrollPosition = sessionStorage.getItem(`scrollPosition_${pagePath}`);
          if (savedScrollPosition) {
              window.scrollTo(0, savedScrollPosition);
          }
      });
      window.addEventListener('DOMContentLoaded',function () {
      const content = document.querySelector('.content');

      if(content){
        window.addEventListener('scroll', () => {
            document.getElementById('logo').style.filter = 'invert(' + Math.min(1 - window.scrollY / 100, 1) + ')';
            });
          }
        });
        
  </script>

  <script>
      window.addEventListener("beforeunload", function () {
        document.body.classList.add("fade-out");
      });
  </script>
</header>
  