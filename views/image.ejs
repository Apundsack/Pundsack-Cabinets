<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= photo.url %></title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="preload" src="<%= photo.url %>" as="image">
  <style>
    .logo {
      filter: invert(0) !important;
    }
    .image-page {
        display: flex;
        justify-content: center; /* Center horizontally */
        padding-bottom: 20px;
    }
    .image-container {
        max-width: 90%; /* Limit image width */
        max-height: 90vh; /* Limit image height */
    }
    .image-container img {
        max-width: 100%;
        max-height: 70vh;
        display: block;
        border-radius: 15px;
    }
    .metadata {
        padding: 0 0 0 20px;
        font-size: 1.1rem;
        line-height: 1.6;
    }
    main{
      padding-top: 0 !important;
    }
  </style>
</head>
<body>
  <%- include('partials/header') %>
  <main>
    <div class="highlight">
    <h1>Image Details</h1>
    </div>
    <div class="content">
      <table style="margin:auto;">
        <tr>
        <td>
          <div class="image-page">
              <div class="image-container">
                  <img src="<%= photo.url %>" alt="<%= photo.url %>">
              </div>
          </div>
        </td>
        <td>
        <div class="metadata">
          <div class="metadata">
            <p><strong>Photo Taken On:</strong> <%= metadata.date %></p>
            <p><strong>Tags:</strong> <%= photo.tags.length > 0 ? photo.tags.join(', ') : 'No tags available' %></p>
            <% if (photo.tags.includes('stained')) { %>
              <button onclick="window.location.href='/about/stain'">Learn About Stain</button>
            <% } %>
            <% if (photo.tags.includes('painted')) { %>
              <button onclick="window.location.href='/about/paint'">Learn About Paint</button>
            <% } %>
            <% if (photo.tags.includes('custom')) { %>
              <button onclick="window.location.href='/about/custom'">Learn About Custom Work</button>
            <% } %>
          </div>
        </div>
      </td>
    </tr>
      </table>
  </main>
  <%- include('partials/footer') %>
</body>
</html>
