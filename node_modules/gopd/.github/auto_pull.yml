name: Auto Pull After Push

on:
  push:
    branches:
      - main  # Replace with the branch you want to monitor

jobs:
  trigger_desktop_pull:
    runs-on: ubuntu-latest 

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Trigger pull on desktop
      run: |
        curl -X GET http://64.255.142.110/:3000/trigger-pull